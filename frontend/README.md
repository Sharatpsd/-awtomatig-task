# Task Manager Frontend

This is a small React + Vite frontend for the Task Manager backend used in the Full Stack Intern assignment.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The app expects the backend API at `http://127.0.0.1:8000/api/tasks/` with the standard CRUD endpoints.
